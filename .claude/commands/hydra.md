Read AGENT_WORKFLOW.md and TASK_CURRENT.md.

Follow the HYDRA workflow exactly:
1. Inspect first.
2. Explain plan before editing.
3. Edit only files needed for TASK_CURRENT.md.
4. Never read .env or secrets.
5. Never commit data/, .venv, node_modules, dist, or untracked files unless explicitly approved.
6. Run scripts/check.ps1 after edits.
7. Show git diff and git status.
8. Commit/push only after validation and explicit approval.

Use Sonnet for normal HYDRA coding tasks.
